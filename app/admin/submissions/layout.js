export const metadata = {
  title: "Admin - Submissions",
  description: "View all medical card eligibility submissions.",
};

export default function AdminLayout({ children }) {
  return children;
}
